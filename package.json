{
  "name": "devtun",
  "version": "0.1.0",
  "description": "Public HTTPS URLs for local Docker containers via Cloudflare Tunnels",
  "type": "module",
  "bin": {
    "devtun": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare",
    "tunnel",
    "traefik",
    "docker",
    "https",
    "development"
  ],
  "files": [
    "dist"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}
